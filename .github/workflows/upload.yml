name: upload artifact
on: [push]
env:
  FILE_NAME: test
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: upload file
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.FILE_NAME }}
          path: .
